export class City {
    connections: City[];
    iata: string;
    latitude: number;
    longitude: number;
    shortName: string;
  
    /* constructor(postalCode: string, county: string, city: string) {
      this.postalCode = postalCode;
      this.county = county;
      this.city = city;
    } */
  }